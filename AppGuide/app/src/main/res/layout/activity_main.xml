<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F5F5F5">

    <FrameLayout
        android:id="@+id/fragment_container"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_weight="1"/>

    <LinearLayout
        android:id="@+id/nav_bar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:background="#FFFFFF"
        android:baselineAligned="false"
        android:divider="@drawable/divider"
        android:showDividers="middle">

        <Button
            android:id="@+id/nav_tab_tuner"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="Tuner"
            android:drawableTop="@drawable/ic_nav_tuner"
            android:gravity="center"
            android:padding="4dp"
            android:textSize="12sp"
            android:background="@drawable/nav_button_bg"/>

        <Button
            android:id="@+id/nav_tab_record"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="Record"
            android:drawableTop="@drawable/ic_nav_record"
            android:gravity="center"
            android:padding="4dp"
            android:textSize="12sp"
            android:background="@drawable/nav_button_bg"/>

        <Button
            android:id="@+id/nav_tab_settings"
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:text="Settings"
            android:drawableTop="@drawable/ic_nav_settings"
            android:gravity="center"
            android:padding="4dp"
            android:textSize="12sp"
            android:background="@drawable/nav_button_bg"/>
    </LinearLayout>
</LinearLayout>
